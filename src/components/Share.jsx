import React, { useState } from 'react';
import './share.css';

const Share = ({ share }) => {

  return (
    <div className="null">
      {/* Not using any more, but cannot delete... */}
    </div>
  );
};

export default Share;